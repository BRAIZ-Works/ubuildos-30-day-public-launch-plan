# Ticket Triage™ v1.0.0 — public product surface

This directory is the Day-03 public product/download **text projection** of the exact Fresh-IQA-passed frozen build. Every product/source member listed in `PUBLICATION_MANIFEST.json` is byte-identical to the frozen `BUILD/` member.

The five carousel PNGs and product screenshot remain separately frozen social assets for the later LinkedIn publication step. They are intentionally not tunneled through opaque/base64 GitHub writes.

Start with `product/index.html`; inspect deterministic rules in `product/rules.json`. Ambiguous, conflicting, empty, or unmatched evidence returns `REVIEW`.
